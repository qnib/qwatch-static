ticker:
    interval: 5000
collector:
  file:
    path: "resources/stdout.log"
    reopen: true
    create: true
filter:
  id:
    inputs: "file,gelf"
handler:
  es_logs:
    host: "172.17.0.1"
    inputs: "id"
  es_events:
    host: "172.17.0.1"
    inputs: "id"
